@model Addition.Models.AdditionViewModel
@ {
    Layout = null;
}
@using(Html.BeginForm())
 {
 < fieldset > < legend style = "color:blue" > Operation Of Two Numbers < /legend>
    @Html.EditorFor(x => x.A)
  @Html.EditorFor(x => x.B) < br / > < br / >
 < input type = "submit" value = "mul" name = "command" / >  
 < h2 style = "color:red" > @Html.Label("Result is :")
    @Html.DisplayFor(x => x.Result) < /h2> < /fieldset>
}  